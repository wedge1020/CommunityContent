// Ralph (Clement83, none specified). A real Wreck-It-Ralph-themed
// building-climbing arcade toy: Ralph automatically climbs a building,
// punching out the weakest window on his current floor (or moving toward
// it) until every window - and finally the rooftop itself - is smashed,
// then he walks off and falls to end the round. The player's own real
// input is genuinely limited to two things: Up/Down/Left/Right pan a
// debug-style camera (`camX`/`camY`, a real, load-bearing upstream
// feature confirmed by reading `ralph.ino`'s own real `loop()` fully -
// not a leftover this port invented an excuse to drop), and Button C at
// any time returns to the title screen. There is no "player controls
// Ralph" input anywhere in real upstream at all - `updateRalph()`'s own
// real targeting/movement logic runs every tick regardless of any button
// state.
//
// Every real `gb.x.y(...)` call site was mechanically rewritten to a
// plain `gbY(...)` function call (this dialect has no classes/methods -
// see gamePong.c's own header comment). `gb.collidePointRect()` ->
// `gbCollidePointRect()`, `gb.display.drawBitmap(x,y,bmp,rot,flip)` ->
// `gbDrawBitmapRotated()` - both real, already-shipped shim primitives.
//
// REAL BITMAP ART: all 16 real upstream sprites actually used by the game
// (`gameover`/`floors`/`GroundFloor`/`RoofTop10`/`50`/`99`/`100`/`idle`/
// `punch`/`walk1`/`walk2`/`windows10`/`50`/`99`/`100`/`titleScreen`) were
// already stored as real `0x`-hex byte literals upstream (no
// `B01111111`-style binary literals needing conversion) - copied
// byte-for-byte into `int[]` arrays with their own real width/height
// headers preserved, every array's real element count computed and
// verified against `width`/`height` before trusting it, not
// hand-transcribed. Real upstream's own `splash_screen` bitmap is dead
// asset data - declared but never actually drawn anywhere in any real
// `.ino` file - and was not ported, matching this project's own
// established "don't port genuinely inert code/assets" precedent.
//
// BLOCKING `gb.titleScreen(F("I'm gonna wreck'it"), titleScreen)` ->
// EXPLICIT STATE: upstream's own real `goToTitleScreen()` (called from
// `setup()` at real boot, and again from `loop()` any time Button C is
// pressed mid-game) blocks on this real widget then immediately calls
// `initGame()` to reset the building/Ralph - ported as `RALPH_STATE_TITLE`
// (drawing the real logo bitmap plus a "PRESS A" hint, this project's own
// established "logo bitmap + press-start hint" convention - see
// gameLander.c's own `landShowTitle()` - rather than reproducing real
// hardware's own internal `Display::titleScreen()` text-centering/overlay
// logic exactly, which isn't available to read from this workspace), the
// same "blocking widget -> explicit resumable state" treatment used
// throughout this project (see gamePong.c's own header comment); a fresh
// Button A press both resets the building/Ralph and starts play, matching
// upstream's own real `goToTitleScreen()->initGame()` call chain exactly.
// Real upstream's own dead `S_INTRO` state (declared, `case S_INTRO:
// state = S_GAME; break;` in the real `switch`) is never actually
// assigned anywhere in real source - not ported, since it does nothing
// upstream either.
//
// Real per-game global state (the `Fenetre fenetres[NB_FENTRES+1]` array
// of `{life,x,y}` structs, and the single `Mobile ralph` struct) was
// flattened to plain parallel `ralphFen*`-prefixed arrays and individual
// `ralphX`/`ralphY`/`ralphStatus` globals, matching every other ported
// game's own established struct-flattening convention.
//
// A real, deliberate switch-fallthrough quirk preserved verbatim, not
// "fixed": `drawRalph()`'s own real `case 3` (climbing between floors)
// has no `break` and falls straight into `case 0` (idle), so real
// hardware draws the idle sprite at the same position TWICE on every
// climbing frame - visually identical to drawing it once (same bitmap,
// same coordinates), but reproduced as a literal double `gbDrawBitmap()`
// call below anyway to stay a faithful byte-for-byte port rather than
// silently dropping upstream's own real (harmless) redundancy. Switch
// fallthrough itself is already proven safe in this dialect - see
// gameStarships101.c's own header comment/explosion-animation code.
//
// Real upstream's own `initWord()`/`updateWord()`/`updateWindow()` are
// genuinely empty function bodies (do nothing at all, called only for
// their own real side-effect-free existence) - not ported, matching this
// project's own "don't port a no-op" precedent. `drawWord()`'s own real
// dead `for` loop (computes `y` every iteration, never reads or uses it)
// was dropped for the same reason. (Upstream's own real "Word"
// spelling - a `world`/`word` typo throughout `world.ino`'s own function
// names - is corrected silently in this port's own internal function
// names only, per this project's own established precedent for
// internal-only typos that are never player-visible - see gameAgaruino.c's
// own header comment on its own `joueurs`/`joueur` normalization.)
//
// Real upstream's own `gb.battery.show = false;` (`initGame()`'s own last
// line) has no equivalent in this shim (no battery-icon HUD element
// exists here at all) - not ported, a real platform-difference omission
// rather than a simplification of anything this port could have kept.
//
// Real upstream's own `S_GAME_OVER` case draws the real `gameover`
// bitmap (88 wide) UNCONDITIONALLY every tick, even the same tick a fresh
// Button A/B press has already reset `state`/re-run `initGame()` - a real,
// harmless one-tick overlap (the very next tick already dispatches to
// `S_GAME` instead) reproduced exactly by calling `ralphInitGame()`
// (which sets `ralphState` itself) without an early `return` before the
// unconditional `gbDrawBitmap()` call, matching upstream's own real
// control flow precisely. The real `gameover` bitmap is genuinely 88px
// wide against this shim's real 84px-wide LCD - a real upstream-authored
// overflow, not a porting mistake - `gbDrawBitmap()` already clips every
// pixel individually (confirmed directly in `gamebuinoShim.c`), so this
// draws identically to real hardware with no risk of an out-of-bounds
// write.

int[530] ralphGameoverBitmap = { 88, 48, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFF, 0xF, , 0x0, 0x7F, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFF, 0xF0, 0x0, 0x7F, 0xBF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFF, 0xE0, 0x0, 0x7F, 0x3F, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFB, 0xC6, 0x3, 0x1E, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFB, 0xC6, 0x3, 0x1E, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFF, 0xC0, 0x0, 0x1D, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xEF, 0xC1, 0x8C, 0x1C, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFF, 0x41, 0x8C, 0x1B, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xC0, 0x40, 0x0, 0x18, 0x17, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xC0, 0xC0, 0x0, 0x10, 0x17, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xE0, 0xC1, 0x8C, 0x10, 0x3F, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xE0, 0xC1, 0x8C, 0x10, 0x3F, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xE0, 0xC1, 0x8C, 0x18, 0x3F, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xE0, 0xC0, 0x0, 0x18, 0x3F, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xE0, 0x40, 0x0, 0x10, 0x3F, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFE, 0x0, 0x0, 0x3, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFE, 0x0, 0x0, 0x3, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFE, 0x0, 0x0, 0x3, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xF8, 0x0, 0x0, 0x0, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xF8, 0x0, 0x0, 0x0, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xF8, 0x30, 0x0, 0x60, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xF8, 0x30, 0x0, 0x60, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xF8, 0xFF, 0xFF, 0xF8, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xF8, 0xFF, 0xFF, 0xF8, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xF8, 0xFF, 0xFF, 0xF8, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xF8, 0xC7, 0xFF, 0x18, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xF8, 0xC7, 0xFF, 0x18, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xF8, 0xF0, 0x0, 0x78, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xF8, 0xF0, 0x0, 0x78, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xF8, 0x70, 0x0, 0x70, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFF, 0x0, 0x0, 0x3, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFF, 0x0, 0x0, 0x3, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFF, 0xC0, 0x0, 0x1F, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFF, 0xC0, 0x0, 0x1F, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFF, 0xC0, 0x0, 0x1F, 0xFF, 0xFF, 0xFF, 0xF0 };
int[187] ralphFloorsBitmap = { 40, 37, 0xF7, 0xFF, 0xFF, 0xFF, 0xDE, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0xF7, 0xFF, 0xFF, 0xFF, 0xDE, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0xF7, 0xFF, 0xFF, 0xFF, 0xDE, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0xF7, 0xFF, 0xFF, 0xFF, 0xDE };
int[57] ralphGroundFloorBitmap = { 40, 11, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x0, 0x0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70, 0x34, 0x0, 0x30, 0x0, 0x58, 0x1C, 0x0, 0x48, 0x0, 0x70, 0x34, 0x0, 0x84, 0x0, 0x58, 0x1C, 0x0, 0x84, 0x0, 0x70, 0x34, 0x0, 0x84, 0x0, 0x58, 0x1C, 0x0, 0x84, 0x0, 0x70 };
int[57] ralphRoofTop10Bitmap = { 40, 11, 0x7C, 0x0, 0x0, 0x3, 0xFC, 0x92, 0x40, 0x0, 0x4A, 0x4A, 0xFF, 0xF8, 0x1, 0x37, 0xFE, 0x82, 0x9, 0x0, 0x60, 0x82, 0x44, 0xA, 0x0, 0xB8, 0x44, 0x34, 0x54, 0x0, 0x24, 0x58, 0x1C, 0xA6, 0x0, 0x42, 0x70, 0x34, 0x9B, 0x0, 0x5A, 0x58, 0x1C, 0xA2, 0x80, 0xE6, 0x70, 0x34, 0xC0, 0x81, 0x80, 0x58, 0x1C, 0x0, 0xFE, 0x0, 0x70 };
int[57] ralphRoofTop50Bitmap = { 40, 11, 0x7F, 0xFC, 0x0, 0x3F, 0xFC, 0x92, 0x49, 0x0, 0x52, 0x4A, 0xFF, 0xFF, 0x83, 0xBF, 0xFE, 0x82, 0x0, 0x44, 0x0, 0x82, 0x44, 0x8, 0x28, 0x38, 0x44, 0x34, 0x54, 0x78, 0x44, 0x58, 0x1C, 0xA2, 0x88, 0x82, 0x70, 0x34, 0x92, 0x94, 0x9A, 0x58, 0x1C, 0x82, 0x82, 0xA6, 0x70, 0x34, 0xFE, 0xFE, 0xC0, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70 };
int[57] ralphRoofTop99Bitmap = { 40, 11, 0x7F, 0xFF, 0xFF, 0xFF, 0xFC, 0x92, 0x49, 0x24, 0x92, 0x4A, 0xFF, 0xFF, 0xFF, 0xFF, 0xFE, 0x82, 0x0, 0x0, 0x0, 0x82, 0x44, 0x38, 0x38, 0x38, 0x44, 0x34, 0x44, 0x44, 0x44, 0x58, 0x1C, 0x82, 0x82, 0x82, 0x70, 0x34, 0x82, 0x82, 0x82, 0x58, 0x1C, 0x82, 0x82, 0x82, 0x70, 0x34, 0xFE, 0xFE, 0xFE, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70 };
int[57] ralphRoofTop100Bitmap = { 40, 11, 0x7F, 0xFF, 0xFF, 0xFF, 0xFC, 0x92, 0x49, 0x24, 0x92, 0x4A, 0xFF, 0xFF, 0xFF, 0xFF, 0xFE, 0x82, 0x0, 0x0, 0x0, 0x82, 0x44, 0x38, 0x38, 0x38, 0x44, 0x34, 0x44, 0x44, 0x44, 0x58, 0x1C, 0x82, 0x82, 0x82, 0x70, 0x34, 0x82, 0x82, 0x82, 0x58, 0x1C, 0x82, 0x82, 0x82, 0x70, 0x34, 0xFE, 0xFE, 0xFE, 0x58, 0x1C, 0x0, 0x0, 0x0, 0x70 };
int[32] ralphIdleBitmap = { 16, 15, 0x2, 0x0, 0x7, 0x40, 0xF, 0xE0, 0xF, 0xE0, 0x1D, 0x70, 0x14, 0x50, 0x32, 0xB8, 0x72, 0x9C, 0xF1, 0x1E, 0xF8, 0x3E, 0x8F, 0xE2, 0xFF, 0xFE, 0xFF, 0xFE, 0xFF, 0xFE, 0xE, 0xE0 };
int[32] ralphPunchBitmap = { 16, 15, 0xF2, 0x1E, 0xD7, 0x56, 0xBF, 0xFA, 0x8F, 0xE2, 0xFB, 0x7E, 0xF8, 0x3E, 0xFA, 0xBE, 0xF2, 0x9E, 0x71, 0x1C, 0x18, 0x30, 0xF, 0xE0, 0xF, 0xE0, 0xF, 0xE0, 0xF, 0xE0, 0xE, 0xE0 };
int[32] ralphWalk1Bitmap = { 16, 15, 0x2, 0x0, 0x7, 0x40, 0xF, 0xE0, 0xF, 0xE0, 0x1D, 0x70, 0x14, 0x50, 0x3A, 0xB8, 0x72, 0x9C, 0xF1, 0x1E, 0xF8, 0x3E, 0x8F, 0xE2, 0xFF, 0xFE, 0xF1, 0xFE, 0xF1, 0xFE, 0xE, 0xE0 };
int[32] ralphWalk2Bitmap = { 16, 15, 0x2, 0x0, 0x7, 0x40, 0xF, 0xE0, 0xF, 0xE0, 0x1D, 0x70, 0x14, 0x50, 0x3A, 0xB8, 0x72, 0x9C, 0xF1, 0x1E, 0xF8, 0x3E, 0x8F, 0xE2, 0xFF, 0xFE, 0xFF, 0x1E, 0xFF, 0x1E, 0xE, 0xE0 };
int[11] ralphWindows10Bitmap = { 8, 9, 0x1C, 0x16, 0x28, 0xC6, 0xA2, 0x12, 0xAA, 0xC6, 0x7C };
int[11] ralphWindows50Bitmap = { 8, 9, 0xFE, 0x86, 0x88, 0xD6, 0xB2, 0x12, 0xAA, 0xC6, 0xFC };
int[11] ralphWindows99Bitmap = { 8, 9, 0xFE, 0x96, 0x98, 0xD6, 0xBE, 0x92, 0x92, 0xD6, 0xFC };
int[11] ralphWindows100Bitmap = { 8, 9, 0xFE, 0x92, 0x92, 0x92, 0xFE, 0x92, 0x92, 0x92, 0xFE };
int[290] ralphLogoBitmap = { 64, 36, 0x0, 0x0, 0x0, 0x3F, 0xFE, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1F, 0xF8, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x7F, 0xFE, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x3C, 0x1E, 0x0, 0x0, 0x0, 0x0, 0x0, 0x7, 0xFB, 0x6F, 0xE0, 0x0, 0x0, 0x0, 0x0, 0x1C, 0xFC, 0x93, 0xFC, 0x0, 0x0, 0x0, 0x0, 0x7C, 0x7D, 0xD3, 0xFF, 0x0, 0x0, 0x0, 0x1, 0xFC, 0x7B, 0xE7, 0xFF, 0xC0, 0x0, 0x0, 0x3, 0xFE, 0x39, 0xCF, 0xFF, 0xC0, 0x0, 0x0, 0x3, 0xFE, 0x30, 0xF, 0xFF, 0xC0, 0x0, 0x0, 0x7, 0xFE, 0x30, 0xEF, 0xFF, 0xE0, 0x0, 0x0, 0x7, 0xFF, 0x11, 0xF, 0xFF, 0xE0, 0x0, 0x0, 0x7, 0xFF, 0x18, 0xF, 0xFF, 0xE0, 0x0, 0x0, 0xF, 0xFF, 0xC, 0x1F, 0xFF, 0xF0, 0x0, 0x0, 0xE, 0xEF, 0xF, 0xFF, 0xFE, 0x70, 0x0, 0x0, 0x8, 0x6F, 0xF, 0xFF, 0xF6, 0x30, 0x0, 0x0, 0x8, 0x3, 0xF, 0xFF, 0xF0, 0x8, 0x0, 0x0, 0x8, 0x3, 0x1F, 0xFF, 0xF0, 0x8, 0x0, 0x0, 0x8, 0x2, 0xF, 0xFF, 0xF0, 0x8, 0x0, 0x0, 0x8, 0x3, 0x5F, 0xFF, 0xF0, 0x8, 0x0, 0x0, 0x8, 0x3, 0x1F, 0xFF, 0xE0, 0x8, 0x0, 0x0, 0x8, 0x3, 0x1F, 0xFF, 0xE0, 0x8, 0x0, 0x0, 0x8, 0x3, 0xFF, 0xFF, 0xE0, 0x8, 0x0, 0x0, 0x8, 0x3, 0xFF, 0xFF, 0xE0, 0x8, 0x0, 0x0, 0x8, 0x3, 0xFF, 0xFF, 0xE0, 0x8, 0x0, 0x0, 0x8, 0x3, 0xFF, 0xFF, 0xE0, 0x8, 0x0, 0x0, 0x8, 0x3, 0xFF, 0xFF, 0xC0, 0x8, 0x0, 0x0, 0x8, 0x1F, 0xFF, 0xFF, 0xFC, 0x8, 0x0, 0x0, 0x8, 0x0, 0xFF, 0xFF, 0xC0, 0x8, 0x0, 0x0, 0x8, 0x0, 0x7F, 0xFF, 0x80, 0x8, 0x0, 0x0, 0x8, 0x0, 0x7F, 0xFF, 0x0, 0x8, 0x0, 0x0, 0x8, 0x0, 0x3F, 0xFE, 0x0, 0x8, 0x0, 0x0, 0x8, 0x0, 0x3F, 0xFE, 0x0, 0x8, 0x0, 0x0, 0x8, 0xC, 0x3F, 0xFF, 0x38, 0x8, 0x0, 0x0, 0x8, 0x37, 0x7F, 0xFF, 0x50, 0x18, 0x0, 0x0, 0x8, 0x50, 0xFF, 0xFF, 0x88, 0x10, 0x0 };

#define RALPH_STATE_TITLE 0
#define RALPH_STATE_GAME 1
#define RALPH_STATE_GAME_OVER 2
#define RALPH_STATE_END_GAME 3

#define RALPH_NB_FENTRES 18
#define RALPH_NB_LINES 6
#define RALPH_LIFE_FENTRE 100
#define RALPH_HIT_RALPH 4

int ralphState;

// Flattened "Fenetre {life,x,y}" array (real upstream NB_FENTRES+1 = 19
// entries - the last one, index RALPH_NB_FENTRES, is the rooftop itself).
int[19] ralphFenLife;
int[19] ralphFenX;
int[19] ralphFenY;

// Flattened "Mobile ralph {status,x,y}".
int ralphX;
int ralphY;
int ralphStatus;

int ralphCamX;
int ralphCamY;

int ralphIndexFenetre;

// Real upstream `int roofLevel(int y)` - 47..36 is ground level, every
// further 12px is one new floor level up.
int ralphRoofLevel( int y )
{
    return ( y - 47 ) / 12;
}

// Real upstream `const byte * chooseSprite(int life)`.
int* ralphChooseSprite( int life )
{
    if( life < 10 ) return ralphWindows10Bitmap;
    if( life < 50 ) return ralphWindows50Bitmap;
    if( life < 99 ) return ralphWindows99Bitmap;
    return ralphWindows100Bitmap;
}

// Real upstream `initWindow()` - 3 windows per line, RALPH_NB_LINES lines,
// plus the rooftop's own real triple-life "window" at the end.
void ralphInitWindow()
{
    int i;
    int y;
    int numFenetre;

    for( i = 0; i < RALPH_NB_LINES; i = i + 1 )
    {
        y = 25 - ( 12 * i );
        numFenetre = i * 3;

        ralphFenLife[ numFenetre ] = RALPH_LIFE_FENTRE;
        ralphFenX[ numFenetre ] = 28;
        ralphFenY[ numFenetre ] = y;

        ralphFenLife[ numFenetre + 1 ] = RALPH_LIFE_FENTRE;
        ralphFenX[ numFenetre + 1 ] = 36;
        ralphFenY[ numFenetre + 1 ] = y;

        ralphFenLife[ numFenetre + 2 ] = RALPH_LIFE_FENTRE;
        ralphFenX[ numFenetre + 2 ] = 44;
        ralphFenY[ numFenetre + 2 ] = y;
    }

    ralphFenLife[ RALPH_NB_FENTRES ] = RALPH_LIFE_FENTRE * 3;
    ralphFenX[ RALPH_NB_FENTRES ] = 36;
    ralphFenY[ RALPH_NB_FENTRES ] = -48;
}

// Real upstream `drawWindow()`.
void ralphDrawWindow()
{
    int i;
    for( i = 0; i < RALPH_NB_FENTRES; i = i + 1 )
    {
        gbDrawBitmap( ralphFenX[ i ] - ralphCamX, ralphFenY[ i ] - ralphCamY, ralphChooseSprite( ralphFenLife[ i ] ) );
    }
}

// Real upstream `drawWord()` (real "World", a typo throughout upstream's
// own `world.ino` - corrected in this port's own internal function name
// only, see this file's own header comment).
void ralphDrawWorld()
{
    int x;

    gbDrawLine( 0, 47 - ralphCamY, 84, 47 - ralphCamY );
    x = 20 - ralphCamX;
    gbDrawBitmap( x, 36 - ralphCamY, ralphGroundFloorBitmap );
    gbDrawBitmap( x, -1 - ralphCamY, ralphFloorsBitmap );
    gbDrawBitmap( x, -37 - ralphCamY, ralphFloorsBitmap );

    if( ralphFenLife[ RALPH_NB_FENTRES ] < 30 )
    {
        gbDrawBitmap( x, -48 - ralphCamY, ralphRoofTop10Bitmap );
    }
    else if( ralphFenLife[ RALPH_NB_FENTRES ] < 180 )
    {
        gbDrawBitmap( x, -48 - ralphCamY, ralphRoofTop50Bitmap );
    }
    else if( ralphFenLife[ RALPH_NB_FENTRES ] < 270 )
    {
        gbDrawBitmap( x, -48 - ralphCamY, ralphRoofTop99Bitmap );
    }
    else
    {
        gbDrawBitmap( x, -48 - ralphCamY, ralphRoofTop100Bitmap );
    }
}

// Real upstream `initRalph()`.
void ralphInitRalph()
{
    ralphY = 32;
    ralphX = 2;
    ralphStatus = 0;
}

// Real upstream `drawRalph()`.
void ralphDrawRalph()
{
    switch( ralphStatus )
    {
    case 3: // climbing between floors
        gbDrawBitmap( ralphX - ralphCamX, ralphY - ralphCamY, ralphIdleBitmap );
        // falls through to case 0 - real upstream double-draw quirk, see
        // this file's own header comment
    case 0: // idle
        gbDrawBitmap( ralphX - ralphCamX, ralphY - ralphCamY, ralphIdleBitmap );
        break;

    case 1: // punch
        if( gbFrameCount % 8 > 3 )
        {
            gbDrawBitmap( ralphX - ralphCamX, ralphY - ralphCamY, ralphPunchBitmap );
        }
        else
        {
            gbDrawBitmap( ralphX - ralphCamX, ralphY - ralphCamY, ralphIdleBitmap );
        }
        break;

    case 2: // walk
        if( gbFrameCount % 8 > 3 )
        {
            gbDrawBitmap( ralphX - ralphCamX, ralphY - ralphCamY, ralphWalk1Bitmap );
        }
        else
        {
            gbDrawBitmap( ralphX - ralphCamX, ralphY - ralphCamY, ralphWalk2Bitmap );
        }
        break;

    case 4: // fall (end-game only) - real upstream cycles all 4 rotations
        if( gbFrameCount % 20 > 14 )
        {
            gbDrawBitmap( ralphX - ralphCamX, ralphY - ralphCamY, ralphIdleBitmap );
        }
        else if( gbFrameCount % 20 > 9 )
        {
            gbDrawBitmapRotated( ralphX - ralphCamX, ralphY - ralphCamY, ralphIdleBitmap, 1, 0 );
        }
        else if( gbFrameCount % 20 > 4 )
        {
            gbDrawBitmapRotated( ralphX - ralphCamX, ralphY - ralphCamY, ralphIdleBitmap, 2, 0 );
        }
        else
        {
            gbDrawBitmapRotated( ralphX - ralphCamX, ralphY - ralphCamY, ralphIdleBitmap, 3, 0 );
        }
        break;
    }
}

// Real upstream `fallRalph()`.
void ralphFallRalph()
{
    if( ralphY < 32 )
    {
        ralphY = ralphY + 1;
    }
    else
    {
        ralphStatus = 0;
    }
}

// Real upstream `updateRalph()` - picks the current target window (the
// weakest one on the lowest still-standing line, or the rooftop once
// every real window is broken), then either punches it, walks toward it,
// or climbs to its floor level.
void ralphUpdateRalph()
{
    int i;

    if( ralphStatus == 1 )
    {
        ralphFenLife[ ralphIndexFenetre ] = ralphFenLife[ ralphIndexFenetre ] - RALPH_HIT_RALPH;

        if( ralphFenLife[ ralphIndexFenetre ] <= 0 )
        {
            ralphFenLife[ ralphIndexFenetre ] = 0;
            ralphStatus = 0;
            if( ralphIndexFenetre == RALPH_NB_FENTRES )
            {
                ralphState = RALPH_STATE_END_GAME;
            }
        }
    }
    else if( ralphStatus == 4 )
    {
        ralphFallRalph();
    }
    else
    {
        for( i = 0; i < RALPH_NB_FENTRES; i = i + 1 )
        {
            if( i == ( RALPH_NB_FENTRES - 1 ) )
            {
                ralphIndexFenetre = i;
                break;
            }
            if( ralphFenLife[ i ] < 30 )
            {
                if( ralphFenLife[ i ] > ralphFenLife[ i + 1 ] )
                {
                    ralphIndexFenetre = i;
                    break;
                }
            }
            else
            {
                ralphIndexFenetre = i;
                break;
            }
        }

        if( ralphFenLife[ ralphIndexFenetre ] <= 0 )
        {
            // every regular window is down - go to the rooftop
            ralphIndexFenetre = RALPH_NB_FENTRES;
        }

        // Ralph goes to the selected window or the rooftop
        if( ralphStatus != 3 && gbCollidePointRect( ralphX + 7, ralphY + 7, ralphFenX[ ralphIndexFenetre ], ralphFenY[ ralphIndexFenetre ], 7, 9 ) )
        {
            ralphStatus = 1;
        }
        else
        {
            if( ralphStatus != 3 && ( ralphX + 4 ) < ralphFenX[ ralphIndexFenetre ] )
            {
                ralphStatus = 2;
                ralphX = ralphX + 1;
            }
            else if( ralphStatus != 3 && ( ralphX + 4 ) > ralphFenX[ ralphIndexFenetre ] )
            {
                ralphStatus = 2;
                ralphX = ralphX - 1;
            }
            else if( ralphRoofLevel( ralphY + 15 ) > ralphRoofLevel( ralphFenY[ ralphIndexFenetre ] ) )
            {
                ralphStatus = 3;
                ralphY = ralphY - 1;
            }
            else if( ralphRoofLevel( ralphY + 15 ) < ralphRoofLevel( ralphFenY[ ralphIndexFenetre ] ) )
            {
                ralphStatus = 3;
                ralphY = ralphY + 1;
            }
            else
            {
                ralphStatus = 0;
            }
        }
    }
}

// Real upstream `updateEndGameRalph()` - Ralph walks right off the
// rooftop, then falls back down to ground level, then the game ends.
void ralphUpdateEndGameRalph()
{
    if( ralphX < 70 )
    {
        ralphX = ralphX + 1;
    }
    else if( ralphY < 32 )
    {
        ralphStatus = 4;
        ralphFallRalph();
    }
    else
    {
        ralphState = RALPH_STATE_GAME_OVER;
    }
}

// Real upstream `initGame()` (real `gb.battery.show = false;` has no
// equivalent here - see this file's own header comment).
void ralphInitGame()
{
    ralphInitWindow();
    ralphInitRalph();
    ralphCamX = 0;
    ralphCamY = 0;
    ralphState = RALPH_STATE_GAME;
}

void ralphUpdateTitle()
{
    gbDrawBitmap( 10, 0, ralphLogoBitmap );
    gbCursorX = 28;
    gbCursorY = 40;
    gbPrintString( "PRESS A" );

    if( gbPressed( BTN_A ) )
    {
        ralphInitGame();
    }
}

void ralphUpdateGameOver()
{
    if( gbPressed( BTN_A ) || gbPressed( BTN_B ) )
    {
        ralphInitGame();
    }
    gbDrawBitmap( 0, 0, ralphGameoverBitmap );
}

void gameRalph_init()
{
    gbBegin();
    ralphState = RALPH_STATE_TITLE;
}

void gameRalph_update()
{
    if( !gbUpdate() ) return;

    if( ralphState != RALPH_STATE_TITLE && gbPressed( BTN_C ) )
    {
        ralphState = RALPH_STATE_TITLE;
    }

    if( ralphState == RALPH_STATE_TITLE )
    {
        ralphUpdateTitle();
    }
    else if( ralphState == RALPH_STATE_GAME )
    {
        ralphDrawWorld();
        ralphDrawWindow();
        ralphDrawRalph();

        ralphUpdateRalph();

        if( gbPressed( BTN_UP ) )
        {
            ralphCamY = ralphCamY - 1;
        }
        else if( gbPressed( BTN_DOWN ) )
        {
            ralphCamY = ralphCamY + 1;
        }

        if( gbPressed( BTN_LEFT ) )
        {
            ralphCamX = ralphCamX - 1;
        }
        else if( gbPressed( BTN_RIGHT ) )
        {
            ralphCamX = ralphCamX + 1;
        }
    }
    else if( ralphState == RALPH_STATE_GAME_OVER )
    {
        ralphUpdateGameOver();
    }
    else if( ralphState == RALPH_STATE_END_GAME )
    {
        ralphDrawWorld();
        ralphDrawWindow();
        ralphDrawRalph();

        ralphUpdateEndGameRalph();
    }

    gbRenderFrame();
}
